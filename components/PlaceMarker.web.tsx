// Web stub — react-native-maps is native-only.
// PlaceMarker is never rendered on web (map.web.tsx handles the web map screen),
// but Metro needs a resolvable web module to avoid bundling the native-only imports.
export function PlaceMarker() {
  return null;
}
